import React, { Fragment } from "react";

import SignalDataGrid from "../components/SignalDatagrid";

const Home = () => (
  <Fragment>
    <SignalDataGrid />
    
  </Fragment>
);

export default Home;
